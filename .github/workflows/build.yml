name: ESLint Workflow Re-Run 4

on: [push]

jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v1
      - name: Install
        run: yarn
      - name: Build ES Lint
        run: yarn build-eslint
